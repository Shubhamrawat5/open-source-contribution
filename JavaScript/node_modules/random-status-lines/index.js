const naval = require('./json/naval.json'); 

const generatenaval = () => {
     const index = Math.floor(Math.random() * naval.length);
     const navalLine = naval[index];

     return navalLine
}
const chanakya = require('./json/chanakya.json'); 

const generatechanakya = () => {
     const index = Math.floor(Math.random() * chanakya.length);
     const chanakyaLine = chanakya[index];

     return chanakyaLine
}
const kapilgupta = require('./json/kapilgupta.json'); 

const generatekapilgupta = () => {
     const index = Math.floor(Math.random() * kapilgupta.length);
     const kapilguptaLine = kapilgupta[index];

     return kapilguptaLine
}
const marcus = require('./json/marcus.json'); 

const generatemarcus = () => {
     const index = Math.floor(Math.random() * marcus.length);
     const marcusLine = marcus[index];

     return marcusLine
}
const nntaleb = require('./json/nntaleb.json'); 

const generatenntaleb = () => {
     const index = Math.floor(Math.random() * nntaleb.length);
     const nntalebLine = nntaleb[index];

     return nntalebLine
}
const seneca = require('./json/seneca.json'); 

const generateseneca = () => {
     const index = Math.floor(Math.random() * seneca.length);
     const senecaLine = seneca[index];

     return senecaLine
}
const socrates = require('./json/socrates.json'); 

const generatesocrates = () => {
     const index = Math.floor(Math.random() * socrates.length);
     const socratesLine = socrates[index];

     return socratesLine
}
const swamiv = require('./json/swamiv.json'); 

const generateswamiv = () => {
     const index = Math.floor(Math.random() * swamiv.length);
     const swamivLine = swamiv[index];

     return swamivLine
}

const rtagore = require('./json/rtagore.json'); 

const generatertagore = () => {
     const index = Math.floor(Math.random() * rtagore.length);
     const rtagoreLine = rtagore[index];

     return rtagoreLine
}

const aboutnaval = require('./json/aboutnaval.json'); 

const generateaboutnaval = () => {
     const index = Math.floor(Math.random() * aboutnaval.length);
     const aboutnavalLine = aboutnaval[index];

     return aboutnavalLine
}
const htgr = require('./json/htgr.json');

const generatehtgr = () => {
     const index = Math.floor(Math.random() * htgr.length);
     const htgrLine = htgr[index];

     return htgrLine
}

module.exports = {generatenaval, generatenntaleb, generatechanakya, generatekapilgupta, generatemarcus, generateseneca, generatesocrates, generateswamiv, generatertagore,generateaboutnaval,generatehtgr};